{{#openAPI.info}}{{>header.hbs}}{{/openAPI.info}}
package {{{packageName}}}

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

@JsonClass(generateAdapter = true)
data class {{{name}}}(
{{#each fields}}
    @Json(name = "{{{rawName}}}")
    val {{{name}}}: {{{type}}}{{#isNullable}}?{{/isNullable}}{{^@last}},{{/@last}}
{{/each}}
){{#each enums}}{{#@first}} { {{~/@first}}
    enum class {{{name}}}(
        val value: {{{type}}}
    ) {
    {{#each options}}
        {{{name}}}({{{value}}}){{^@last}},{{/@last}}
    {{/each}}
    }
{{#@last~}} } {{~/@last}}{{/each}}
